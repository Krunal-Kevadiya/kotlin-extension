buildscript {
    apply from: './settings/dependencies.gradle'
    addRepos(repositories)

    dependencies {
        classpath deps.other.plugin
        classpath deps.kotlin.plugin
    }
    repositories {
        mavenCentral()
    }
}

allprojects {
    addRepos(repositories)
    addProjectSetting()
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

